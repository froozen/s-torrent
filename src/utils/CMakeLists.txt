file ( GLOB_RECURSE UTILS_FILES ${CMAKE_CURRENT_LIST_DIR} *.cpp )
add_library ( utils ${UTILS_FILES} )
find_package ( JsonCpp REQUIRED )
include_directories ( JsonCpp_INCLUDE_DIRS )
target_link_libraries ( utils ${JsonCpp_LIBRARIES} )
